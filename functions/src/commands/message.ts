// import { bot } from '../bot';

export const initialize = () => {
  // copy every message and send to the user
  // bot.on("message", (ctx) => {
  //   ctx.telegram.copyMessage(ctx.chat.id, ctx.chat.id, ctx.message.message_id)
  //
  //
  // });

  // bot.hears('/list', (ctx) => ctx.reply('/list'))
};
